@model IEnumerable<Grasews.Web.ViewModels.Ontology_MvcViewModel>

@{
    ViewBag.Title = string.Format(WebResource.OntologiesCreatedBy, ViewBag.LoggedInUserFullName);
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h3>@ViewBag.Title</h3>
<hr />
@if (Model != null && Model.Count() > 0)
{
    <table class="table table-striped">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OntologyName)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.OntologyName)
                </td>
                <td class="text-right">

                    <a href="@Url.Action("Details", new { idOntology = item.Id })" class="btn btn-sm btn-primary">
                        <span class="fa fa-file-code-o" aria-hidden="true"></span>&nbsp;@WebResource.Details
                    </a>
                    <a href="@Url.Action("Delete", new { idOntology = item.Id })" class="btn btn-sm btn-danger">
                        <span class="fa fa-remove" aria-hidden="true"></span>&nbsp;@WebResource.Delete
                    </a>
                    @*<a href="@Url.Action("Index", "Ontology_User", new { idOntology = item.Id })" class="btn btn-sm btn-warning">
                        <span class="fa fa-users" aria-hidden="true"></span>&nbsp;@WebResource.SharedUsers
                    </a>*@
                </td>
            </tr>
        }

    </table>
}

<script>
    $(function () {
        $("#grasews-admin-navbar li").removeClass("active");
        $("#nav-ontologies").addClass("active");
    });
</script>